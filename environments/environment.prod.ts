/**
 * Production environment configuration.
 *
 * Set `apiUrl` to the deployed backend proxy's origin. `apiKey` stays empty
 * for the same reason as in development — see environment.ts.
 */
export const environment = {
  production: true,
  apiUrl: '',
  apiKey: '',
};
